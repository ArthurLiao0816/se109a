# deno ~
---
`deno run [js file]`
* 用deno跑javaScript檔案

`deno run [options] [js file]`
* 各種options
    * `-A` : 提供所有權限
    * `--allow-net` : 網路權限
    * `--allow-read` : 讀取權限
    * `--allow-write` : 寫入權限